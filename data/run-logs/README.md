# Immutable daily run logs

Each scheduled discovery run writes exactly one new `<run_id>.json` file here. `data/discovery-log.json` is frozen legacy history and is no longer rewritten by the daily automation.
